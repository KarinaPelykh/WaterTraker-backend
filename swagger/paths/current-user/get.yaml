tags:
  - Auth
summary: signin current user without email and password
operationId: signInCurrentUser
description: Sign in current user
security:
  - bearerAuth: []
responses:
  "200":
    description: "Get current user"
    content:
      application/json:
        schema:
          type: object
          properties:
            name:
              type: string
              example: Jou Doe
            email:
              type: string
              example: test@gmail.com
  "401":
    $ref: "../../components/responses/401.yaml"
